pydantic>=2.12.5
graphviz>=0.21
anthropic
python-dotenv
pydantic-ai-slim[anthropic,duckduckgo,logfire]>=1.58.0
# Multi-agent runtime for the math_conversation domain. Plain `crewai`
# (NOT crewai[anthropic]): the crew runs on OpenAI — CrewAI's core
# provider, openai>=2.30 pulled transitively — which sidesteps the
# anthropic-SDK version clash with pydantic-ai-slim (crewai[anthropic]
# pins anthropic~=0.73 vs our >=0.97). Pinned to the 1.14.x line: the
# native-provider API and provider pins shift between CrewAI minors, so
# bump deliberately. Worker image only; the API never imports the domain.
# See docs/math_conversation.md → Dependency strategy.
crewai>=1.14.5,<1.15
streamlit-agraph
openpyxl>=3.1.3
reportlab>=4.4.7,<5
b2sdk
psycopg[binary,pool]>=3.2
celery[redis]>=5.4
python-docx
fastapi>=0.115.0
uvicorn>=0.34.0
httpx>=0.28.0
python-multipart
pymupdf
pytest
mkdocs>=1.6
mkdocs-material>=9.5
mkdocstrings[python]>=0.25