# file: links must be hard-copied for Next/Turbopack to resolve them.
# Without this, `@aiplatform/sdk` is symlinked and Turbopack's resolver
# emits "module not found" even when Node-level resolution succeeds.
install-links=true
