# Dependency set for the `crewai` worker runtime (WORKER_RUNTIME=crewai).
#
# This stack runs the math_conversation domain only. It CANNOT coexist
# with the default runtime in one interpreter: CrewAI pins
# opentelemetry-sdk <1.35 while Logfire (pydantic-ai-slim[logfire])
# needs >=1.39. So this runtime drops the `logfire` extra and runs on a
# separate worker pool. See ai_platform/jobs/runtimes.py and
# docs/math_conversation.md → Dependency strategy.
#
# Verified resolvable (uv): 206 packages, opentelemetry-sdk 1.34.1.
# Kept parallel to requirements.txt minus `logfire`, plus `crewai`.

pydantic>=2.12.5
graphviz>=0.21
anthropic
python-dotenv
# No `logfire` extra here — it conflicts with crewai over opentelemetry.
pydantic-ai-slim[anthropic,duckduckgo]>=1.58.0
# Multi-agent runtime. Plain `crewai` (crew runs on OpenAI, CrewAI's core
# provider) pinned to the 1.14.x line; the native-provider API shifts
# between minors, so bump deliberately. Needs OPENAI_API_KEY at runtime.
crewai>=1.14.5,<1.15
streamlit-agraph
openpyxl>=3.1.3
reportlab>=4.4.7,<5
b2sdk
psycopg[binary,pool]>=3.2
celery[redis]>=5.4
python-docx
fastapi>=0.115.0
uvicorn>=0.34.0
httpx>=0.28.0
python-multipart
pymupdf
pytest
mkdocs>=1.6
mkdocs-material>=9.5
mkdocstrings[python]>=0.25
